{% extends 'main.twig' %}

{% block content %}
    <div class="uk-column-1-1">
        <h1>{{ page.title }}</h1>
        <div class="page-content">
            {{ page.body }}
        </div>
    </div>
{% endblock %}